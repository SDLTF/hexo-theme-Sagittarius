
<!DOCTYPE html>
<html lang="<%=config.language%>">

<head>
  <meta charset="UTF-8">
  <title><%=theme.website_title%></title>
  <% if (theme.stylesheets !== undefined && theme.stylesheets.length > 0) { %>
  <!-- stylesheets list from _config.yml -->
  <% theme.stylesheets.forEach(url => { %>
  <link rel="stylesheet" href="<%=url%>">
  <% }); %>
  <% } %>
  
<% if(theme.background_image.enable){ %>
  <%- '<style>body{background-image: url(\"' +  theme.background_image.img_list[Math.abs(Math.ceil((Math.random() * theme.background_image.img_list.length) - 1))]  + '\") }</style>'%>
  
  <% }%>
</head>

<body>


  <div id="menu-outer">
    <div id="blog_title">
      <p><%= theme.blog_title %></p>
    </div>
    <div id="menu-inner">
      <% Object.keys(theme.menu).forEach(key => { %>
      <a href="<%=theme.menu[key]%>"><%=key%></a>
      <% }); %>
    </div>
  </div>

  <div id="main">
    <%- body %>
  </div>

  <!-- <div id="bottom-outer">
  <div id="bottom-inner">
    Site by <%=config.author%> using
    <a href="http://hexo.io">hexo blog framework</a>.
    <br>
    <a href="<%=config.root%>">Home</a>
  </div>
</div> -->

  <% if (theme.scripts !== undefined && theme.scripts.length > 0) { %>
  <!-- scripts list from theme config.yml -->
  <% theme.scripts.forEach(url => { %>
  <script src="<%=url%>"></script>
  <% }); %>
  <% } %>

</body>

</html>